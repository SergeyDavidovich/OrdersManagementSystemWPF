<Window x:Class="OMS.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="False"
        Title="{Binding Title}" Height="800" Width="1200" WindowStartupLocation="CenterScreen" WindowState="Maximized">
    <Grid Background="LightGray">
        <Grid.Resources>
            <Style TargetType="Border">
                <Setter Property="Background" Value="White"/>
                <Setter Property="Margin" Value="10"/>
                <Setter Property="CornerRadius" Value="10"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect Color="Black" Opacity="0.7" ShadowDepth="1"/>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Border>
            <ContentControl FontSize="46" Margin="10">
                <ContentControl.Content>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="HOME" Margin="20,0,50,0"/>
                        <TextBlock >
                            <Run Text="{Binding Title}"/>
                            <Run Text=" - "/>
                            <Run Text="{Binding State}"/>
                        </TextBlock>
                    </StackPanel>
                </ContentControl.Content>
            </ContentControl>
        </Border>
        <Border Grid.Row="1">
            <ContentControl prism:RegionManager.RegionName="ContentRegion">
                <!--<ContentControl.Content>
                    <TextBlock Text="Static Content" HorizontalAlignment="Center" FontSize="40"/>
                </ContentControl.Content>-->
            </ContentControl>
        </Border>
    </Grid>
</Window>
