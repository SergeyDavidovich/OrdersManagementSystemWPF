<UserControl x:Class="Entities.Views.ManageEntityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Entities.Views"
             xmlns:prism="http://prismlibrary.com/"
           xmlns:inf="clr-namespace:Infrastructure;assembly=Infrastructure"
           mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <ContentControl Margin="10" prism:RegionManager.RegionName="{x:Static inf:RegionNames.EntityListRegion}"></ContentControl>
        </ScrollViewer>
        <ScrollViewer Grid.Column="1" Margin="10">
            <ContentControl prism:RegionManager.RegionName="{x:Static inf:RegionNames.EntityViewAddEditRegion}"></ContentControl>
        </ScrollViewer>
    </Grid>
</UserControl>
